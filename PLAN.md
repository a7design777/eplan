# eplan — план і структура

Планувальник маршрутів для електромобілів по Європі. Деплой: Cloudflare Worker → **e.car-ua.com**.
Правила роботи — [CLAUDE.md](CLAUDE.md).

## Що робить застосунок

Користувач задає старт, фініш і проміжні точки, обирає авто, стартовий і цільовий заряд, фільтрує конектори/мережі/безкоштовні зарядки — і отримує:
- маршрут із зупинками на зарядку та часом кожної зупинки;
- загальний час у дорозі (їзда + зарядки) і кілометраж;
- попередження про платні дороги з альтернативним безкоштовним маршрутом;
- кнопки «відкрити у Waze / Google Maps» для маршруту і для кожної зарядки;
- збереження маршрутів в акаунті (email+пароль).

## Структура

```
eplan/
  CLAUDE.md                 # правила — читати перед змінами
  PLAN.md                   # цей файл — оновлювати після кожного етапу
  wrangler.jsonc            # Worker, assets, D1, KV, cron
  migrations/               # SQL-міграції D1
  data/vehicles.json        # сід ~30 моделей EV
  src/                      # backend (Hono на Worker)
    index.ts                # монтує /api/*, роздає static assets
    auth/                   # реєстрація, логін, сесії, requireAuth
    routing/
      valhalla.ts           # клієнт Valhalla: throttle + KV-кеш (єдина точка виходу)
      consumption.ts        # модель споживання: швидкість, підйом, температура
      charge-curve.ts       # інтегрування кривої зарядки → час зупинки
      planner.ts            # підбір зарядних зупинок (ядро)
      tolls.ts              # детекція платних ділянок + альтернатива
    stations/
      query.ts              # вибірка станцій у коридорі маршруту (geohash)
      import.ts             # cron-імпорт з OpenChargeMap
  web/                      # frontend (Vite + React + MapLibre)
```

**D1**: `users`, `sessions`, `saved_routes`, `stations`, `networks`, `connector_types`.
**KV**: кеш Valhalla (TTL 7 днів), кеш геокодування.

**Індексація станцій**: колонка geohash-префікса (7 символів) з індексом. Вибірка «станції в коридорі» = набір префіксів уздовж полілінії → один запит `WHERE geohash_prefix IN (...)`. Без цього D1 робить full scan на ~150k рядків при кожному плануванні.

## Алгоритм планування (`src/routing/planner.ts`)

1. Базовий маршрут через Valhalla `/route` з `elevation_interval`.
2. Симуляція споживання вздовж полілінії: базове Wh/km × коефіцієнти швидкості (квадратично від ~90 км/год), підйому (потенційна енергія + рекуперація з втратами), температури → крива SoC по дистанції.
3. Жадібний підбір зупинок: іти вперед, поки SoC не впаде до резерву (за замовч. 10%); шукати кандидатів у коридорі ~5 км серед досяжних; скоринг за потужністю, довжиною об'їзду, фільтрами користувача, кількістю портів.
4. Час зарядки: інтегрувати криву від SoC прибуття до SoC, потрібного щоб дістатись наступної точки з резервом (не «до 80%» наосліп) + ~5 хв на під'їзд і підключення.
5. Перерахунок маршруту з зупинками як via-точками (справжні об'їзди), за потреби один повтор кроків 2–4.
6. Врахувати цільовий SoC на фініші — остання зарядка може бути довшою.

**Платні дороги**: другий запит із `exclude_tolls: true`. Якщо основний маршрут має платні ділянки — показати обидва варіанти з різницею в часі й км; для безплатного зарядки перераховуються заново. Показуємо *наявність* платних ділянок і країни, не суму (Valhalla тарифів не дає).

**Deep links**: Waze `waze.com/ul?ll=lat,lon&navigate=yes` (по одній точці — Waze не тримає multi-stop). Google Maps `google.com/maps/dir/?api=1&origin=…&destination=…&waypoints=a|b|c`, ліміт 9 проміжних — якщо зупинок більше, ріжемо на кілька посилань.

## Етапи

| # | Етап | Статус |
|---|---|---|
| 0 | Node у `~/.local`, PATH у `.zshrc` | ✅ |
| 0 | CLAUDE.md + PLAN.md | ✅ |
| 1 | Скелет: wrangler, Hono, D1+KV біндінги | ✅ (деплою ще не було) |
| 2 | Схема D1 + міграції + сід `data/vehicles.json` (36 моделей) | ✅ |
| 3 | Імпорт станцій з OCM + cron | ✅ 31 870 станцій, 42 країни |
| 4 | Клієнт Valhalla: throttle, KV-кеш, `RoutingProvider` | ✅ |
| 5 | `consumption.ts` + `charge-curve.ts` + юніт-тести | ✅ |
| 6 | `planner.ts` + детекція платних доріг | ✅ |
| 7 | Frontend: мапа, форма, підсумок, deep links | ✅ |
| 10 | Ціни й спосіб оплати на станціях | ✅ |
| 11 | П'ять стилів мапи, вибір точок кліком, старт по GPS | ✅ |
| 8 | Auth + збережені маршрути | ✅ |
| 9 | Прив'язка домену `e.car-ua.com` | ✅ живий, HTTPS працює |

Прод: **https://e.car-ua.com** (дубль для перевірок: https://eplan.a7design.workers.dev)
Акаунт Cloudflare `a7design@gmail.com`, D1 `bfd7d49c-3950-4d08-9113-59b6197ceea5`, KV `9089a3ccff0b46d985d34dc4c7be00b3`.

### Що перевірено у проді на реальних даних
- Берлін → Мюнхен, Kia EV6: 588 км, 2 зупинки (EnBW Eisenberg, Tesla Feucht), 43 хв зарядки, фініш 15 %.
- Мадрид → Барселона, Tesla Model S 70D: 621 км, 3 зупинки, 145 хв зарядки, фініш 16 %.
- Виключення мереж працює: після виключення обраних мереж план дає інші станції (493 хв замість 509).
- Париж → Ліон: платні 412 км; альтернатива без них +28 км і +2.5 год.
- Auth: реєстрація, вхід, вихід, ізоляція між користувачами.
- 68 юніт-тестів, `tsc --noEmit` чистий.

### Поріг потужності при імпорті
Дзеркало тягне станції **від 3 кВт**, а не від 50. Поріг у 50 кВт відсікав дві третини
реальних станцій — у коридорі Альмуньєкар — Малага їх було 88 проти 226 від 11 кВт,
а на перших 40 км від Альмуньєкара жодної. Планувальник через це не бачив зарядку
поруч і вів у об'їзд за місто. База: 133 486 станцій, з них 31 871 від 50 кВт
і 16 397 з побутовою розеткою 220 В.

### Вибір станції зважує час, а не потужність
Різниця між 150 і 250 кВт — кілька хвилин, між 150 і 11 кВт — години. Тому в скорингу
стоїть штраф `0.3 × ємність / потужність` у годинах, а не бонус за кВт. Без цього
планувальник ставив зупинку на 11 кВт лише тому, що вона трапилась ближче,
і спокійно планував там 288 хвилин.

### Дані станцій
`npm run stations:fetch` (потрібен `OCM_API_KEY` у середовищі) → `data/stations.sql` (~10 МБ, у git не зберігається)
→ `npm run stations:import`. Далі cron щопонеділка о 03:00 оновлює змінене через `modifiedsince`.
Ключ у секретах Worker'а: `wrangler secret put OCM_API_KEY`.

### Ціни й оплата
- Ціна — поле `UsageCost` з OCM, вільний текст. Показуємо дослівно, не парсимо.
  Заповнена приблизно у 13.5k станцій із 31.9k.
- Спосіб оплати виводиться з `UsageTypeID` — єдиного структурованого поля про доступ.
  Розподіл: membership 16.4k, public 7.7k, pay_at_location 3.2k, немає даних 4.1k.
- Де даних немає — пишемо «спосіб оплати не вказано», а не вигадуємо.

### Стратегії зарядки
`chargingStrategy` у фільтрах керує стелею заряду й довжиною відрізка:

| Стратегія | Стеля | Мін. відрізок | Ціль відрізка |
|---|---|---|---|
| `fewest_stops` | 95 % | 40 км | якнайдалі |
| `balanced` | 85 % | 25 км | якнайдалі |
| `short_stops` | 70 % | 60 км | ~130 км |

Стеля діє лише поки попереду є зупинки — на останній набирається рівно цільовий SoC.
Заміряно на Берлін → Мюнхен (EV6): 2 зупинки / 53 хв, 2 / 43 хв, 4 / 46 хв відповідно.
**«Часті короткі» тут не швидші за «збалансовано»** — виграш у потужності з'їдається
накладними на кожну зупинку. Стратегія має сенс там, де мережа щільна, а зарядки повільні.

### Улюблені мережі
`preferredNetworkIds` — бонус у скорингу (+0.8), а не жорсткий фільтр: якщо станції
улюбленої мережі поруч немає, маршрут не ламається. Мережу не можна одночасно
любити й виключати — UI знімає конфлікт автоматично.

### Альтернативні маршрути
У Valhalla просимо `alternates: 2`. Кожен варіант рахується як окремий план,
у UI перемикач. Рушій не гарантує, що дасть альтернативи взагалі.

### Стилі мапи
**Рельєф** і **Супутник** — відкриті тайли Esri, вибір у localStorage.
Ключа не потребують, але це чужий сервіс: під помітне навантаження або комерційне
використання Esri вимагає акаунт. Перше місце під заміну, якщо проєкт виросте.

`setStyle` стирає додані шари, а подія `styledata` після підміни приходить не завжди —
тому маршрут відновлюється активною перевіркою протягом кількох секунд, а не по події.
Та сама причина, що й у `ensureRouteLayers`: на подію `load` теж покладатись не можна.

### Відомі обмеження моделі даних
- Конектор `type2` не розрізняє AC Type 2 (11–22 кВт) і Tesla DC Type 2. Для старих
  європейських Tesla (Model S/X до CCS-ретрофіту) це може дати хибні зупинки.
  Пом'якшено тим, що імпортуємо лише станції ≥50 кВт.

### Продуктивність і надійність
- **Кеш планів** — повна відповідь `/api/plan` лежить у KV годину. Ключ будується
  вже з підставленою температурою, тож план із живою погодою не змішується з ручним.
- **Прогрес** — `/api/plan/stream` віддає етапи через SSE. EventSource не годиться:
  він уміє лише GET, а запит на планування великий і йде тілом POST.
- **Ліміт станцій** — запитуємо на одну більше за ліміт; якщо прийшла, кажемо про це.
- **Альтернативні маршрути** не рахуються понад 900 км: чотири повних проходи
  вичерпували процесорний час Worker'а і давали 503.

### Димовий тест
`npm run smoke` ганяє 7 реальних маршрутів проти живого сервера і падає з ненульовим
кодом. Вбудований у `npm run deploy`. Потрібен тому, що обидва падіння з 503 були
через ліміти Cloudflare — юніт-тести таке не ловлять.
`SMOKE_URL=http://localhost:8787 npm run smoke` — проти локального.

## Перевірка

- `npm test` — детерміновані тести `consumption.ts`, `charge-curve.ts`, `planner.ts` без мережі.
- Звірка трьох реальних маршрутів з abetterrouteplanner.com, розбіжність за кількістю зупинок і часом у межах ~15%:
  - Берлін → Мюнхен (просто, ~1 зупинка);
  - Варшава → Барселона (довгий, багато зупинок, платні дороги);
  - авто з малою батареєю (Zoe) — стрес-тест підбору.
- Візуальна перевірка dev-сервера: мапа рендериться, маркери на місцях, Waze/GMaps посилання ведуть на правильні координати.
- Auth: реєстрація → логін → збереження → вихід → вхід → маршрут на місці; чужий маршрут не читається.

## Відкриті питання

- Ціни на зарядку: OCM дає їх нерегулярно — спершу орієнтир за kWh.
- Суми платних доріг недоступні безкоштовно — показуємо лише наявність.
- Публічний Valhalla — demo-сервер під fair use. План Б при зростанні трафіку: свій Valhalla на VPS для Європи або OpenRouteService з ключем.
